{
  "name": "financial-manager",
  "version": "2.0.0",
  "private": true,
  "description": "Sistema de Gestão Financeira com Email Marketing",
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend-api run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend-api run dev",
    "server": "npm --prefix backend-api run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
